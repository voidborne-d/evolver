## 1. 🚨 Title Mismatch Error (页面标题不匹配)
- **报错现象:** 测试在第一步 `setupProject` 就全部挂掉。
- **修复方案:** 更新 `babylon-fixtures.ts` 中的 Title 检查。

## 2. 📛 Entity Name Mismatch (实体名称不匹配)
- **报错现象:** 无法找到 "Box", "Sphere" 等元素。
- **修复方案:** 全局替换为 "Box Mesh", "Sphere Mesh" 等。

## 3. 👻 Missing Entity in Context (上下文实体缺失)
- **报错现象:** `updateComponent` 测试失败。
- **修复方案:** 显式创建 "Box Mesh" 以供测试。