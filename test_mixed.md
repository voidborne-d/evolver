# Single vs Multi Test

Single line: `console.log("hello")` (This usually fails in card)

Multi line:
```javascript
console.log("hello");
console.log("world");
```
(This might work if we use `interactive` message with `markdown` tag instead of `plain_text`?)

Let's test `lark_md` tag in `interactive` card specifically.