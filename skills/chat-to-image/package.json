{
  "name": "chat-to-image",
  "version": "1.0.0",
  "description": "Fetch Feishu chat history, render to HTML, and (future) convert to image.",
  "main": "fetch_and_render.js",
  "scripts": {
    "fetch": "node fetch_and_render.js",
    "send": "node send_image.js"
  },
  "dependencies": {
    "commander": "^9.0.0",
    "dotenv": "^16.0.0"
  },
  "author": "OpenClaw Evolution"
}
